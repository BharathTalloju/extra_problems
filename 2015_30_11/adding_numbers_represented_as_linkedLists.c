#include <stdio.h>
#include <stdlib.h>

typedef struct node{
	int digit;
	struct node *next;
}NODE;

int add_linkedList_store_in_result(NODE *list1,NODE *list2,NODE *result){
	int sum;
	
	if(!((list1-> next) || (list2-> next))){
		sum = (list1-> digit) + (list2-> digit);
		add_() 
	}
}

int add_linkedList(NODE *list1,NODE *list2){
	int 
}

int main(){
	
}